{
  "version": "coding-v1",
  "fixtures": [
    {
      "id": "clamp",
      "instruction": "Implement clamp(value, low, high) in solution.py. Return only the requested JSON object.",
      "candidate_file": "solution.py",
      "fixture_files": {
        "test_solution.py": "from solution import clamp\n\ndef test_inside(): assert clamp(5, 1, 9) == 5\ndef test_low(): assert clamp(-2, 1, 9) == 1\ndef test_high(): assert clamp(20, 1, 9) == 9\n"
      },
      "test_argv": ["pytest", "-q"],
      "expected_content": "def clamp(value, low, high):\n    return max(low, min(value, high))\n"
    },
    {
      "id": "unique_ordered",
      "instruction": "Implement unique_ordered(values) in solution.py without sorting. Return only the requested JSON object.",
      "candidate_file": "solution.py",
      "fixture_files": {
        "test_solution.py": "from solution import unique_ordered\n\ndef test_values(): assert unique_ordered([3, 1, 3, 2, 1]) == [3, 1, 2]\ndef test_empty(): assert unique_ordered([]) == []\n"
      },
      "test_argv": ["pytest", "-q"],
      "expected_content": "def unique_ordered(values):\n    return list(dict.fromkeys(values))\n"
    }
  ]
}
